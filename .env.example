# Copy to .env (gitignored) and fill in. Run every command from the repo root.
OPENROUTER_API_KEY=
# google/embeddinggemma-300m is gated on Hugging Face: accept its license, then use a read token.
HF_TOKEN=
EMBED_URL=http://localhost:8001

# Optional
# QDRANT_PATH=qdrant_data
# LLM_MODEL=google/gemini-2.5-flash
# RERANK_URL=
